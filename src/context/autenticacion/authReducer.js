import React from "react";

import {
  REGISTRO_ERROR,
  LOGIN_EXITOSO,
  CERRAR_SESION,
  LOGIN_ERROR,
  REGISTRO_EXITOSO,
  OBTENER_DATOS,
} from "../../types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
